@using Todo.Data.Entities
@using Todo.Extensions
@using Todo.Services
@model Todo.Models.TodoLists.TodoListDetailViewmodel

@{
    ViewData["Title"] = $"List: {Model.Title}";
}

<h2>@ViewData["Title"]</h2>

<div class="col-md-6">
    <div class="row">
        <div class="col-xs-6">
            <a asp-action="Create" asp-controller="TodoItem" asp-route-todoListId="@Model.TodoListId">
                <strong>Add New Item</strong>
            </a>
        </div>
        <div class="col-xs-6">
            @await Html.PartialAsync("DetailsFilterPartial", Model.Filter)
        </div>
    </div>

    <table id="todo-list" class="table table-striped table-bordered" style="">
        <thead>
        <tr>
            <th>Title</th>
            <th>Responsible</th>
            <th>Importance</th>
            <th>Rank</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.Items.OrderBy(x => x.Importance.ToInt()))
        {
            string contextualClass;
            switch (item.Importance)
            {
                case Importance.High:
                    contextualClass = "danger";
                    break;
                case Importance.Low:
                    contextualClass = "info";
                    break;
                default:
                    contextualClass = "";
                    break;
            }
            <tr class="@contextualClass">
                <td>
                    <a asp-action="Edit" asp-controller="TodoItem" asp-route-todoItemId="@item.TodoItemId">

                        @if (item.IsDone)
                        {
                            <s>@item.Title</s>
                        }
                        else
                        {
                            <text>@item.Title</text>
                        }
                    </a>
                </td>
                <td class="text-right">
                    <small>
                        @item.ResponsibleParty.UserName

                        <img src="https://www.gravatar.com/avatar/@Gravatar.GetHash(item.ResponsibleParty.Email)?s=30"/>

                    </small>
                </td>
                <td>@item.Importance.ToInt()</td>
                <td>@item.Rank</td>
            </tr>
        }
        </tbody>
    </table>
</div>